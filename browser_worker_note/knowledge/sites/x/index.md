# X (旧Twitter) サイトナレッジ

## 基本情報

- **URL**: https://x.com
- **アカウント**: @UaW6wnKW8c87193（「チームAアイドル」アイディアに時間を、タスクはAIへ｜株式会社ユニゾンテクノロジー）
- **ログインURL**: https://x.com/i/flow/login
- **認証方式**: メール+パスワード / Google / Apple
- **ログインフロー**: メール入力 → 「次へ」 → パスワード入力 → 「ログイン」（2ステップ）

## ナビゲーション構造

### サイドメニュー（常時表示）

| 項目 | URL | 備考 |
|------|-----|------|
| ホーム | /home | タイムライン |
| 検索 | /explore | |
| 通知 | /notifications | |
| フォローする | /i/connect_people | おすすめユーザー |
| ダイレクトメッセージ | /i/chat | |
| Grok | /i/grok | AI機能 |
| プレミアム | /i/premium_sign_up | 有料プラン |
| プロフィール | /UaW6wnKW8c87193 | |
| その他 | — | ボタン（展開メニュー） |
| ポストする | /compose/post | |

### ホームタイムライン

| タブ | 説明 |
|-----|------|
| おすすめ | アルゴリズム推薦 |
| フォロー中 | フォロー中ユーザーの時系列 |

### ポスト作成（インライン）

ホーム画面上部にインラインポストフォームあり:
- `textbox "ポスト本文"` — テキスト入力
- 画像や動画を追加ボタン
- GIF画像を追加ボタン
- Grokでブラッシュアップボタン
- 投票を追加ボタン
- 絵文字を追加ボタン
- ポストを予約ボタン
- 位置情報を追加ボタン（disabled）
- コンテンツ開示ボタン
- 「ポストする」ボタン（テキスト入力後にenabled）

### 右サイドバー

- 検索バー
- プレミアム宣伝
- 本日のニュース
- 速報（トレンド）
- おすすめユーザー

## 探索状況

- [x] ログインフロー（実績: セッション1,6）
- [x] ホーム（/home）（実績: セッション1）
- [x] インラインポストフォーム（実績: セッション6 — 即時投稿成功）
- [x] ポスト作成モーダル（/compose/post）（実績: セッション7 — 予約投稿成功）
- [x] 予約投稿設定（/compose/post/schedule）（実績: セッション7）
- [x] 予約投稿一覧（/compose/post/unsent/scheduled）（実績: セッション13 — 予約編集成功）
- [x] プロフィールページ（/UaW6wnKW8c87193）（実績: セッション — プロフィール編集成功）
- [x] プロフィール編集（/settings/profile）（実績: セッション — 名前・Bio・ヘッダー・アイコン更新成功）
- [ ] 設定画面 — **優先度: 低**。運用上の必要性が出てから探索
- [ ] アナリティクス — **優先度: 高**。x-post-report タスクの精度向上に直結。個別ポスト分析の自動化時に探索

## ポスト作成モーダル（/compose/post）

「ポストする」リンクまたはサイドメニューから開く。

### 構造

| 要素 | ref例 | 操作 |
|------|-------|------|
| 閉じる | e24 | モーダルを閉じる |
| 下書き | e32 | 下書き一覧 |
| ポスト本文 | e76 | textbox — テキスト入力 |
| 全員が返信できます | e85 | 返信権限の変更 |
| 画像や動画を追加 | e100 | ファイルアップロード |
| GIF画像を追加 | e106 | GIF選択 |
| Grokでブラッシュアップ | e111 | AI支援 |
| 投票を追加 | e116 | アンケート機能 |
| 絵文字を追加 | e122 | 絵文字ピッカー |
| **ポストを予約** | e127 | 予約設定画面へ |
| 位置情報を追加 | disabled | 無効 |
| コンテンツ開示 | e137 | |
| ポストする | e143 | テキスト入力後にenabled |

### 予約投稿設定（/compose/post/schedule）

「ポストを予約」クリックで表示。**プレミアム不要で利用可能**。

| 要素 | 型 | 備考 |
|------|-----|------|
| 月 | combobox | 1月〜12月 |
| 日 | combobox | 1〜31 |
| 年 | combobox | 2026〜2028 |
| 時 | combobox | 0〜23（24時間表記） |
| 分 | combobox | 00〜59（1分刻み） |
| Calendarボタン | button | カレンダーUI |
| タイムゾーン | 表示のみ | 日本標準時 |
| 確認する | button | 予約設定を確定してポスト作成に戻る |
| 予約投稿ポスト | button | 一覧表示（フッター） |

操作手順:
```
1. select_option "時" → 時を選択     ← ★時間を先に設定
2. select_option "分" → 分を選択
3. select_option "月" → 月を選択     ← 日付は後
4. select_option "日" → 日を選択
5. select_option "年" → 年を選択
6. click "確認する" → ポスト作成画面に戻る
7. ポスト本文を入力
8. click "予約設定する" → 予約完了
```

**⚠ 時間→日付の順序が必須**: 日付を先に変更すると、現在時刻より前の時間が設定されたままになり「過去の日時にポストを予約することはできません」エラーが発生する。時間を先に設定すればこのエラーを回避できる。

### 予約投稿一覧・編集（/compose/post/unsent/scheduled）

予約済みポストの確認・編集が可能。

| 要素 | 型 | 備考 |
|------|-----|------|
| 予約投稿ポスト一覧 | button群 | 各予約ポストがボタンとして表示 |
| scheduledTweetIndicator | button | 編集モーダル内の日時表示（クリックで日時変更） |
| クリア | button | 予約を解除 |
| 更新 | button | 日時変更を確定（新規作成時の「確認する」に相当） |

編集手順:
```
1. navigate /compose/post/unsent/scheduled → 予約一覧
2. click 対象ポストのボタン → 編集モーダル表示
3. click scheduledTweetIndicator → 日時設定画面
4. select_option で日時変更（時間→日付の順）
5. click "更新" → モーダルに戻る
6. 必要に応じて本文・画像を修正
7. click "予約設定" → 更新完了
```

## 実証済みフロー（最適化パス）

### 即時投稿（画像付き）— セッション6で成功

```
1. navigate /home
2. snapshot → インラインフォームの textbox ref を取得
3. type（slowly: true）→ 本文入力
4. click "画像や動画を追加" → ファイルチューザー検出
5. file_upload → 画像パス指定
6. snapshot → 画像プレビュー確認
7. [人間承認]
8. click "ポストする"
9. snapshot → alert "ポストを送信しました。" + 投稿URLで成功確認
```

所要ステップ: 9（うち snapshot 3回）

### 予約投稿（画像付き）— セッション7で成功、セッション13で最適化

```
1. navigate /compose/post → モーダル表示
2. click "ポストを予約" → 日時設定画面
3. select_option "時" → 時を選択     ← ★時間を先に
4. select_option "分" → 分を選択
5. select_option "月" / "日" / "年"  ← 日付は後
6. click "確認する" → モーダルに戻る
7. type（slowly: true）→ 本文入力
8. click "画像や動画を追加" → ファイルチューザー検出
9. file_upload → 画像パス指定
10. [人間承認]
11. click "予約設定"
12. snapshot → alert "ポストの送信日時: ..." で成功確認
```

所要ステップ: 12（うち snapshot 1回 + 必要に応じて追加）

### バッチ予約投稿（連続スケジューリング）— セッション13で実証

複数ポストを連続予約する場合のパターン。1セッションで7件連続成功。

```
[ポスト1]
1. navigate /compose/post
2. click "ポストを予約" → 日時設定
3. select_option 時→分→月→日→年（時間先行）
4. click "確認する"
5. type（slowly: true）→ 本文
6. click "画像や動画を追加" → file_upload（画像ありの場合）
7. click "予約設定"
8. snapshot → 成功確認

[ポスト2〜N]
9. navigate /compose/post ← 毎回新規モーダルを開く
10. 手順2〜8を繰り返す
```

**注意**: 前のポストの予約完了後、モーダルは閉じてホームに戻る。次のポストは必ず `/compose/post` に再遷移すること。

### 共通の注意点

- **日本語入力**: `browser_type` に `slowly: true` 必須
- **画像添付**: click → file_upload の2ステップ。ファイルチューザーは自動検出される
- **成功検出**: alert 要素。即時="ポストを送信しました。"、予約="ポストの送信日時: ..."
- **ref値は毎回変わる**: snapshot で都度取得。ハードコードしない
- **overlay クリックブロック回避**: モーダル内のボタンが `<div>` overlay に遮られて `browser_click` がタイムアウトする場合がある。`browser_evaluate` で `document.querySelector('[data-testid="<testid>"]').click()` を使えば直接クリック可能。よく使う testid:
  - `scheduledTweetIndicator` — 予約日時表示ボタン
  - `tweetButton` — 「予約設定」ボタン
- **予約日時の順序**: 必ず時間（時→分）を先に設定してから日付（月→日→年）を設定する。逆順だと過去日時エラー

## プロフィール編集（/settings/profile）

プロフィールページの「プロフィールを編集」リンクまたは `/settings/profile` へ直接遷移。
モーダルダイアログとして表示される。

### 構造

| 要素 | 型 | 備考 |
|------|-----|------|
| バナー画像を追加 | button | ヘッダー画像アップロード |
| Choose File (banner) | button | 隠しファイル入力 |
| Imagineで編集 | button | AI画像編集 |
| 画像を削除 | button | ヘッダー画像削除 |
| プロフィール画像を追加 | button | アイコンアップロード |
| 名前 | textbox | 最大50文字 |
| 自己紹介 | textbox (textarea) | 最大160文字 |
| 場所 | textbox | |
| ウェブサイト | textbox | |
| 生年月日 | button | 変更画面へ遷移 |
| 保存 | button | 変更を保存 |

### 実証済みフロー — プロフィール更新

```
1. navigate /UaW6wnKW8c87193 → プロフィールページ
2. click "プロフィールを編集" → 編集モーダル表示
3. click "バナー画像を追加" → ファイルチューザー
4. file_upload → ヘッダー画像パス
5. click "適用" → クロップ確定、モーダルに戻る
6. click "プロフィール画像を追加" → ファイルチューザー
7. file_upload → アイコン画像パス
8. click "適用" → クロップ確定、モーダルに戻る
9. click 名前テキストボックス → Ctrl+A → type で上書き
10. 自己紹介は evaluate で直接値セット（slowly: true だとタイムアウト）
11. click "保存" → プロフィールページに戻る
```

### 注意点

- **自己紹介(textarea)**: `browser_type` の `slowly: true` はタイムアウトする。`browser_evaluate` で `nativeInputValueSetter` + `input` イベントで直接値セットする
- **画像クロップ**: アップロード後に「メディアを編集」ダイアログ（スライダー付き）が表示される。デフォルト位置でよければ「適用」を押すだけ
- **名前の上限**: 50文字。カウンター表示あり
- **自己紹介の上限**: 160文字。カウンター表示あり

## 注意事項

- **ログインフロー**: 2段階（メール入力画面 → パスワード入力画面）。各画面でのref値はセッションごとに変わる
- **2要素認証ダイアログ**: ログイン直後に「アカウントのセキュリティを強化する」が出る場合あり。「閉じる」で回避
- **初回ロード**: SPAのため5秒待機推奨
- **ポスト文字数制限**: 140文字（プレミアムなら拡張可能）
- **beforeunloadなし**: noteと違い、X側では離脱時のダイアログなし
- **予約投稿**: プレミアム不要。comboboxで日時指定
- **ブラウザセッション**: context断絶後もブラウザは維持される。再ログイン不要の場合あり
